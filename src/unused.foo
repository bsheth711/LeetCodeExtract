// unused graphql query
const graphqlFilePath = './src/graphql/profile.graphql';

const QUERY = readFileSync(graphqlFilePath).toString();

fetch("https://leetcode.com/graphql/", {
	method: "POST",
	headers: {
		"Content-Type": "application/json",
		"LEETCODE_SESSION": CREDS.LEETCODE_SESSION,
		"csrftoken": CREDS.csrftoken
	},
	body: JSON.stringify({
		query: QUERY,
		variables: {username: CREDS.username}
	})
}).then((response) => response.json())
  .then((data) => console.log(data));